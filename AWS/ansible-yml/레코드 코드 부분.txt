- name: Add an alias record that points to an Amazon ELB
  amazon.aws.route53:
    state: present
    zone: foo.com
    record: elb.foo.com
    type: A
    value: "{{ elb_dns_name }}"
    alias: true
    alias_hosted_zone_id: "{{ elb_zone_id }}"

https://docs.ansible.com/ansible/latest/collections/amazon/aws/route53_module.html#ansible-collections-amazon-aws-route53-module
load_balancer_arn
canonical_hosted_zone_id
dns_name